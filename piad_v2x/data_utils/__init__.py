"""Dataset loading and per-message feature computation.

Handles the VeReMi Extension benchmark: leakage-safe sender-grouped splits and the
kinematic and interaction feature extraction that the detectors consume.
"""
